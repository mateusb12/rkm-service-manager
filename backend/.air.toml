root = "."
tmp_dir = "tmp"

[build]
  bin = "tmp/main"
  cmd = "go build -o ./tmp/main ."
  delay = 500
  exclude_dir = ["tmp"]
  include_ext = ["go"]
  kill_delay = "500ms"
  log = "build-errors.log"
  send_interrupt = true

[log]
  time = true
